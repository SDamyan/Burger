<h1>Eat-Da-Burger!</h1>

<h2>Burgers</h2>

<ul>
  {{#each burgers}}
    {{#unless devouredy}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Burgers</h2>

<ul>
  {{#each burgers}}
    {{#if devouredy}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devouredy">Devoured?</label><br>
    <input type="radio" name="devouredy" value="1" checked> Devoured!<br>
    <input type="radio" name="devouredy" value="0"> Ready to Eat
  </div>

  <button type="submit">Add Burger</button>
</form>
